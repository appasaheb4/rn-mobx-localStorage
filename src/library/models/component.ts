export enum ElementSize {
  Auto,
  Block,
}

export enum StackDirection {
  Horizontal = 'row',
  Vertical = 'column',
}

export enum BorderLocation {
  All,
  TopOnly,
  BottomOnly,
  LeftOnly,
  RightOnly,
  None,
  Circle,
}

export enum Location {
  All,
  Horizontal,
  Vertical,
  Top,
  Bottom,
  Left,
  Right,
  None,
}
